<Window x:Class="ABC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:ABC.Views"
        mc:Ignorable="d"
        Title="AEP Barcode Companion"
        Height="900" Width="1182"
        MinHeight="750" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}">

    <Window.Resources>
        <Style TargetType="TabControl" BasedOn="{StaticResource ModernTabControl}" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main content area with tab control and theme toggle -->
        <Grid Grid.Row="0">
            <TabControl Margin="8,8,8,4">
                <TabItem Header="USB Download">
                    <views:UsbDownloadView DataContext="{Binding UsbDownload}" />
                </TabItem>
                <TabItem Header="Bluetooth Live Scan">
                    <views:BluetoothLiveView DataContext="{Binding BluetoothLive}" />
                </TabItem>
                <TabItem Header="Range Maker">
                    <views:RangeMakerView DataContext="{Binding RangeMaker}" />
                </TabItem>
                <TabItem Header="Scanner Settings">
                    <views:ScannerSettingsView DataContext="{Binding ScannerSettings}" />
                </TabItem>
            </TabControl>

            <!-- Theme toggle button overlaid at top-right of the tab strip -->
            <Button Command="{Binding ToggleThemeCommand}"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,10,12,0"
                    Padding="8,4" FontSize="16"
                    ToolTip="Toggle dark/light theme">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                        <Setter Property="Content" Value="🌙" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDarkTheme}" Value="True">
                                <Setter Property="Content" Value="☀" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="1" Background="{DynamicResource StatusBarBackground}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource StatusBarForeground}" />
            </StatusBarItem>
            <Separator />
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal"
                            ToolTip="Total number of barcodes across all tabs">
                    <TextBlock Text="Barcodes: " Foreground="{DynamicResource StatusBarForeground}" />
                    <TextBlock Text="{Binding TotalBarcodeCount}" Foreground="{DynamicResource StatusBarForeground}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
